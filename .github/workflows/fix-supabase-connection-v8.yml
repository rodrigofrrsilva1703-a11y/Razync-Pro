name: Fix Supabase Connection V8
on:
  push:
    paths:
      - scripts/fix_supabase_connection_v8.py
jobs:
  apply:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python scripts/fix_supabase_connection_v8.py
      - run: python -m py_compile database.py app.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add database.py
          git diff --cached --quiet || git commit -m "corrige conexao persistente com Supabase"
          git push
