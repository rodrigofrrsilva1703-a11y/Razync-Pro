name: Rebuild Intelligent Interface
on:
  push:
    paths:
      - scripts/rebuild_interface.py
      - ui_system.py
jobs:
  apply:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python scripts/rebuild_interface.py
      - run: python -m py_compile app.py ui_system.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app.py
          git diff --cached --quiet || git commit -m "reconstroi experiencia visual e dashboard inteligente"
          git push
