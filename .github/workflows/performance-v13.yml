name: Performance V13
on:
  push:
    paths:
      - scripts/performance_v13.py
      - .github/workflows/performance-v13.yml
jobs:
  apply:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python scripts/performance_v13.py
      - run: python -m py_compile database.py app.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add database.py app.py
          git diff --cached --quiet || git commit -m "aplica Performance V13"
          git push
